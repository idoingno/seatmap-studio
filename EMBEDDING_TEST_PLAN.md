1. **创建新矩阵布局**

   - 打开应用 http://localhost:8089
   - 拖动"矩阵布局"到画布
   - 创建一个 5x5 的矩阵
   - 验证默认情况下所有子节点（椅子）是否正常显示

2. **测试整体移动（子节点跟随父节点）**

   - 点击矩阵的空白区域选中整个 matrixContainer
   - 拖动 matrixContainer
   - **关键验证**: 所有椅子、标签是否跟随移动？
   - 如果启用 embedding 后仍然正常移动，说明 embedding 没有冲突
   - 如果出现异常（子节点闪烁、分离），可能需要调整 embedding 配置

3. **测试子节点独立移动**

   - 尝试单独拖动一个椅子
   - 预期行为：应该无法移动（因为 disableMove: true）
   - 如果可以移动了，说明 embedding 可能影响了禁用逻辑

4. **测试圆桌布局**

   - 创建一个圆桌布局
   - 重复测试 2 和 3

5. **测试添加/删除行列**

   - 使用 AddMenuNode 添加一行/一列
   - 使用 MinusMenuNode 删除一行/一列
   - 验证操作后整体移动是否仍然正常

6. **测试框选和移动多个座位**

   - Ctrl/⌘ 框选多个座位
   - 拖动选中的座位
   - 验证移动是否流畅

7. **性能测试**
   - 创建一个大矩阵（如 20x20）
   - 测试整体移动的性能
   - 观察是否有卡顿或延迟

=== ⚠️ 可能的问题 ===

如果启用 embedding 后出现以下问题：

1. **子节点不跟随父节点移动**

   - 可能原因：embedding 的 findParent 逻辑需要调整
   - 解决方案：修改 findParent 函数，确保正确识别父节点

2. **移动时子节点闪烁**

   - 可能原因：setChildren 和 embedding 存在冲突
   - 解决方案：选择使用其中一种机制，或调整 embedding 配置

3. **性能下降**

   - 可能原因：embedding 需要实时计算 bbox 交集
   - 解决方案：禁用 embedding 或优化 findParent 函数

4. **拖拽失败**
   - 可能原因：embedding 影响了事件处理
   - 解决方案：检查 embedding 配置中的其他选项

=== 📋 如果遇到问题，需要收集的信息 ===

1. 具体的操作步骤
2. 预期行为 vs 实际行为
3. 浏览器控制台的错误信息
4. 性能数据（是否有卡顿）
5. 矩阵大小（rows x columns）
