// 椅子尺寸
export const CHAIR_SIZE: number = 40;

// 椅子间隔尺寸
export const SPACE_SIZE: number = 10;

// 组内边距尺寸
export const PARENTTOPANDBOTTOMHEIGHT: number = 30;
export const PARENTLEFTANDRIGHTSPACE = 60;

// 走廊尺寸
export const AISLE_SIZE: number = 60;
export const AISLE_DEFAULT_SIZE: number = 6;

// 偏移距离
export const MATRIX_OFFSET_DISTANCE: number = CHAIR_SIZE + AISLE_DEFAULT_SIZE + SPACE_SIZE / 2;
export const MATRIX_OFFSET_SIZE_DISTANCE: number = CHAIR_SIZE + AISLE_SIZE + SPACE_SIZE / 2;

// 圆桌父节点间隙
export const PARENT_EXTRA_SPACE = 100;

// 开始角度
export const CHAIR_START_ANGLE = 0;
